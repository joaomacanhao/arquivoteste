sudo apt-get install git

ls -a para verificar se git ta instalado

git conig --global user.name "leonardomacanhao"

git config --global user.email "email"joao.macanhao.pb@compasso.com.br"

ls -a para verificar existencia do .gitconfig

git add nome do arquivo

git commit

git diff (para verificar as alteracoes)

criar a chave ssh no site do github

de volta ao terminal

ssh-keygen (ira pedir o nome o gerar sozinho caso nao de)

eval 'ssh-agent'

ssh-add nomedachaveprivada

dentro da pasta que for fazer commit

git remote add origin linksshdorepo

git branch (para saber o nome da master)

git push origin master
